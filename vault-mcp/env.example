# Vault MCP Server Configuration

# Required: Vault token (use least-privileged token; avoid root tokens)
VAULT_TOKEN=hvs.your-token-here

# Optional: Vault server address (default: http://127.0.0.1:8200)
VAULT_ADDR=http://127.0.0.1:8200

# Optional: Allow read operations (default: true)
VAULT_ALLOW_READ=true

# Optional: Allow write operations (default: false)
VAULT_ALLOW_WRITE=false

# Optional: Comma-separated list of allowed paths; when unset, all paths are allowed (NOT RECOMMENDED)
# VAULT_ALLOWED_PATHS=secret/,kv/production/

# Optional: Restrict local filesystem operations to this directory (recommended for YAML import/export)
# VAULT_ALLOWED_WORKING_DIR=/home/user/vault-backups

# Optional: Log level (debug|info|error). DEBUG=true is also supported to enable debug level
# LOG_LEVEL=info
# DEBUG=false
