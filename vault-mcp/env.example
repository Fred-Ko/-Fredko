# Vault MCP Server Configuration

# Required: Vault root token
VAULT_TOKEN=hvs.your-root-token-here

# Optional: Vault server address (default: http://127.0.0.1:8200)
VAULT_ADDR=http://127.0.0.1:8200

# Optional: Allow read operations (default: true)
VAULT_ALLOW_READ=true

# Optional: Allow write operations (default: false)
VAULT_ALLOW_WRITE=false

# Optional: Comma-separated list of allowed paths
# If not set, all paths are allowed
# VAULT_ALLOWED_PATHS=secret/,kv/production/